<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Ideatap" %></title>

    <%= stylesheet_link_tag "application" %>
    <%= csrf_meta_tags %>

  </head>

  <body>
    <!-- Navbar -->
    <%= render 'layouts/navbar' %>


    <%= yield %>

    <script type="text/javascript">
    // Set global api paths
    window.g = {};
    g.userIdeasUrl = '<%= user_idea_api_path(current_user) %>';
    </script>
    <%= javascript_include_tag "application" %>
    <%= yield :javascript %>
  </body>
</html>