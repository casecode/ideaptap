<%= form_for @user do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div><%= f.label :name %>
  <%= f.text_field :name %></div>
  <br />

  <div><%= f.label :email %>
  <%= f.email_field :email %></div>
  <br />

  <div><%= f.label :admin %>
  <%= f.check_box :admin %></div>
  <br />

  <div><%= f.label "Current wallet" %>
  <%= f.number_field :wallet %>
  <br />

  <div class="actions"><%= f.submit "Edit User", :class => 'button small alert radius' %></div>
<% end %>
